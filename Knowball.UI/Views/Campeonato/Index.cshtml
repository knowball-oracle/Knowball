@using Knowball.UI.ViewModels
@model List<CampeonatoViewModel>

@{
    ViewData["Title"] = "Campeonatos";
}

<div class="container-fluid mt-4">
    <div class="row mb-4">
        <div class="col-md-8">
            <h1><i class="bi bi-trophy"></i> Gerenciar Campeonatos</h1>
        </div>
        <div class="col-md-4 text-end">
            <a asp-action="Create" class="btn btn-success">
                <i class="bi bi-plus-circle"></i> Novo Campeonato
            </a>
        </div>
    </div>

    @if (Model == null || Model.Count == 0)
    {
        <div class="alert alert-info" role="alert">
            <i class="bi bi-info-circle"></i> Nenhum campeonato cadastrado.
            <a asp-action="Create">Clique aqui</a> para criar um novo.
        </div>
    }
    else
    {
        <div class="table-responsive">
            <table class="table table-hover table-striped">
                <thead class="table-success">
                    <tr>
                        <th>ID</th>
                        <th>Nome</th>
                        <th>Ano</th>
                        <th>Categoria</th>
                        <th style="width: 150px;">Ações</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.IdCampeonato</td>
                            <td>@item.Nome</td>
                            <td>@item.Ano</td>
                            <td>@item.Categoria</td>
                            <td>
                                <a asp-action="Edit" asp-route-id="@item.IdCampeonato"
                                   class="btn btn-sm btn-warning">
                                    <i class="bi bi-pencil"></i> Editar
                                </a>
                                <a asp-action="Delete" asp-route-id="@item.IdCampeonato"
                                   class="btn btn-sm btn-danger">
                                    <i class="bi bi-trash"></i> Deletar
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
</div>
